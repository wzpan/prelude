# -*- mode: snippet -*-
# name: btn
# key: btn
# --
{% btn /images/`(mfa/get-directory-from-bufname (buffer-file-name))`/${1:$$(yas/choose-value (mfa/yield-choices (buffer-file-name)))} ${2:text} ${3:blue} %}